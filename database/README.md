# Base de datos

Este directorio se usa para guardar el archivo SQLite generado en tiempo de ejecución.

Archivo esperado:

```text
database/ecommerce.db
```

El archivo `.db` no se versiona porque se genera automáticamente al inicializar la Etapa 3.
